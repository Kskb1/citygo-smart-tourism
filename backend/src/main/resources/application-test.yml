spring:
  datasource:
    url: jdbc:h2:mem:citygo;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false

citygo:
  security:
    jwt-secret: test-only-citygo-secret-with-enough-length
    token-ttl-hours: 2
    admin-username: admin
    admin-password: 123456
    admin-email: admin@citygo.local
    demo-users-enabled: true
